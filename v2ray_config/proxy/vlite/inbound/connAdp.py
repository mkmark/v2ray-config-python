from pydantic.dataclasses import dataclass, Field as field
from typing import Optional

# import v2ray_config.common.buf as buf
# import v2ray_config.common.signal.done as done


# @dataclass(slots=True)
# class udpConnAdp(Conn):
#     reader: Optional[Reader] = field(default_factory=Reader)
#     cached_multi_buffer: Optional[MultiBuffer] = field(default_factory=MultiBuffer)
#     finished: Optional[Instance] = field(default_factory=Instance)
